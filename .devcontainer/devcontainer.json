{
    "name": "PADE",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "postCreateCommand": "pip install -e '.[dev]' && pip install cocotb jupyter",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-toolsai.jupyter"
            ]
        }
    }
}
